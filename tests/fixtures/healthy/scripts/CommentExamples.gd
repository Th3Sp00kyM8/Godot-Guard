extends Node

# This comment mentions res:// path to a texture, but it is prose, not a concrete resource.
# This comment also mentions `res://docs/example.tscn`.

const EXISTING_PATH := "res://scenes/Main.tscn"

func _ready() -> void:
	pass
